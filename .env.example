# Memory MCP Server Configuration

# Port to expose (default: 3000)
MCP_PORT=3000

# Path to memories directory on host machine
# Use absolute path or relative path from docker-compose.yml location
# Examples:
#   - ./memories (relative, creates in current directory)
#   - /var/mcp/memories (absolute path on Linux/Mac)
#   - C:\mcp\memories (absolute path on Windows)
MEMORIES_PATH=./memories

# Node environment
NODE_ENV=production
